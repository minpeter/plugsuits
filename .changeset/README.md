This directory stores changeset files for package versioning and npm releases.

Each markdown file in this directory records package bumps and release notes.
The release workflow uses these files to open version PRs and publish packages.
