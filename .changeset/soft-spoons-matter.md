---
"@ai-sdk-tool/harness": patch
---

Document the new harness DX for observing every stream part, surfacing buffered assistant text before tool calls, and using the non-streaming `Agent.generate()` path.
